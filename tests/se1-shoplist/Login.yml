info:
  name: Login
  type: http
  seq: 2

http:
  method: POST
  url: "{{hostUrl}}/auth/login"
  body:
    type: json
    data: |-
      {
        "email": "pavel@example.com",
        "password": "1234"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
