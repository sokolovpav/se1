info:
  name: Get all lists
  type: http
  seq: 3

http:
  method: GET
  url: "{{hostUrl}}/lists"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
