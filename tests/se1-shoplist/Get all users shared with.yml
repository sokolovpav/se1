info:
  name: Get all users shared with
  type: http
  seq: 13

http:
  method: GET
  url: "{{hostUrl}}/share/1"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
