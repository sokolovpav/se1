info:
  name: Get list items
  type: http
  seq: 7

http:
  method: GET
  url: "{{hostUrl}}/lists/1/items"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
