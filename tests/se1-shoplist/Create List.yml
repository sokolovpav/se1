info:
  name: Create List
  type: http
  seq: 4

http:
  method: POST
  url: "{{hostUrl}}/lists"
  body:
    type: json
    data: |-
      {
        "name": "New List"
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
